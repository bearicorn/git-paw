//! Project initialization.
//!
//! Implements `git paw init` — creates `.git-paw/` directory, generates
//! default config, manages `.gitignore`, and injects into `AGENTS.md`.

// TODO: implement in feat/init-command
