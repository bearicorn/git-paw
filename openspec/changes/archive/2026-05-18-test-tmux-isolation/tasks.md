## 1. Add the shared helpers in `tests/helpers/mod.rs`

- [x] 1.1 Add `pub struct TmuxTestEnv` that owns a `TempDir` (typically created with `tempfile::TempDir::new()` inside `std::env::temp_dir()`). Expose a public `new()` constructor and a public `socket_dir(&self) -> &Path` accessor for diagnostics.
- [x] 1.2 Add a public method `pub fn apply<'a>(&self, cmd: &'a mut Command) -> &'a mut Command` that calls `cmd.env("TMUX_TMPDIR", self.socket_dir.path()).env_remove("TMUX").env_remove("TMUX_PANE")` and returns `cmd` for chaining. Document on the method that callers MUST keep the `TmuxTestEnv` alive for at least as long as the spawned tmux server.
- [x] 1.3 Add a public free function `pub fn tmux_test_env() -> TmuxTestEnv` (thin constructor wrapper) so callers can write `let env = helpers::tmux_test_env();` and then `env.apply(&mut cmd);`.
- [x] 1.4 Add `pub fn guard_against_live_session()` that runs `Command::new("tmux").arg("ls").output()` against the user's default socket (i.e. without applying `tmux_test_env`), inspects stdout for lines whose session-name field starts with `paw-`, and panics with a message naming the offending session(s), recommending `tmux kill-session -t <name>` or `cargo test --test <name>`, and naming the `GIT_PAW_ALLOW_LIVE_SESSION=1` escape hatch.
- [x] 1.5 In `guard_against_live_session()`, treat a non-zero exit from `tmux ls` whose stderr contains "no server running" (or whose stdout is empty) as "no live sessions" and return without panicking. Honour `GIT_PAW_ALLOW_LIVE_SESSION=1` by returning unconditionally.
- [x] 1.6 Update `setup_test_repo()` to call `guard_against_live_session()` as its first statement, before creating the sandbox tempdir.
- [x] 1.7 Add Rustdoc on `TmuxTestEnv`, `tmux_test_env`, and `guard_against_live_session` documenting the invariant ("every tmux-spawning test MUST apply this helper") and cross-referencing this change name in a `// see openspec/changes/test-tmux-isolation` comment.

## 2. Add unit tests for the new helpers

- [x] 2.1 Add `tests/helpers/mod.rs::tests::tmux_test_env_sets_expected_env_vars` — construct a `TmuxTestEnv`, apply it to a `Command::new("/usr/bin/env")`, capture stdout, assert `TMUX_TMPDIR` is set to a path inside the helper's `socket_dir()`, and assert `TMUX` and `TMUX_PANE` are absent from the child env. Behavioural: tests the output of `env`, not the internals of the struct.
- [x] 2.2 Add `tests/helpers/mod.rs::tests::guard_returns_when_no_tmux_server` — apply a fresh `TmuxTestEnv` to a `Command::new("tmux").arg("ls")`, prove the test-owned socket has no server, then call the guard (the guard reads the **default** socket; if the dev machine running the test has no `paw-*` session on the default socket, the guard returns). To make this deterministic, use the same trick as in `guard_panics_when_live_paw_session_exists` below: temporarily set `TMUX_TMPDIR` on the **current process** (via `std::env::set_var` under `serial_test`) to a known-empty test socket dir for the duration of the test, restore the prior value on Drop, and assert the guard returns. Use `serial_test::serial` to avoid concurrent env-var races.
- [x] 2.3 Add `tests/helpers/mod.rs::tests::guard_panics_when_live_paw_session_exists` — under `serial_test::serial`, set `TMUX_TMPDIR` on the current process to a freshly-created test socket dir, create a `paw-guard-test` session on that dir via `tmux new-session -d -s paw-guard-test`, call `guard_against_live_session()` inside `std::panic::catch_unwind`, assert it panicked, assert the panic message contains `paw-guard-test`, kill the test session, restore `TMUX_TMPDIR`.
- [x] 2.4 Add `tests/helpers/mod.rs::tests::guard_honours_allow_live_session_env` — under `serial_test::serial`, set `TMUX_TMPDIR` to a socket dir containing a `paw-*` session AND set `GIT_PAW_ALLOW_LIVE_SESSION=1`, call the guard, assert it returns without panicking.
- [x] 2.5 Verify the new helper tests compile with `cargo build --tests`. `serial_test` is already a dev-dependency per `AGENTS.md`'s approved set; no new deps.

## 3. Audit and update every test that spawns tmux

- [x] 3.1 Run `grep -n 'new-session\|tmux::new_session\|Command::new("tmux")' tests/*.rs` and list every site that constructs a tmux `Command`. Expected files: `tests/auto_approve_integration.rs`, `tests/prompt_inbox_integration.rs`, plus any direct invocations in `tests/e2e_tests.rs`, `tests/recover_integration.rs`, `tests/boot_block_integration.rs`. Apply `helpers::tmux_test_env()` to each: bind a local `let tmux_env = helpers::tmux_test_env();` near the top of the test, then `tmux_env.apply(&mut cmd)` on every `tmux` `Command` builder. (Tests that exercise in-process library code use `apply_to_process()` under `#[serial]` instead, since `apply()` only configures a `Command` builder.)
- [x] 3.2 Run `grep -n 'args(\["start"\]\|args(\["start",' tests/*.rs` to list every `git paw start` invocation. Expected files: `tests/e2e_tests.rs`, `tests/boot_block_integration.rs`, `tests/from_specs_launch_fixes_integration.rs`, `tests/recover_integration.rs`, `tests/start_force_integration.rs`, `tests/supervisor_integration.rs`. For each: bind a `let tmux_env = helpers::tmux_test_env();` and apply it to the `cmd()` builder before `.args(["start", ...])`. (Pure `--dry-run` invocations in `cross_format_spec_selection.rs`, `from_specs_launch_fixes_integration.rs`, `start_force_integration.rs`, and `supervisor_integration.rs` never spawn tmux and were left as-is.)
- [x] 3.3 Apply `tmux_test_env()` to every related `git paw` subcommand call in the same test that interacts with the same session — `git paw stop`, `git paw recover`, `git paw purge --force`, `git paw dashboard --once`, `git paw inject` — using the **same** `TmuxTestEnv` instance as the `start` call so the subprocesses share a tmux server.
- [x] 3.4 Apply `tmux_test_env()` to every direct `tmux` `Command` invocation used for assertions (e.g. `tmux capture-pane`, `tmux list-panes`, `tmux kill-session`, `tmux detach-client`) so those assertions see the test-owned socket, not the default socket.
- [x] 3.5 For each test file touched, run `cargo test --test <name>` after the edits to prove the test still passes against the per-test socket. Verified: `auto_approve_integration`, `boot_block_integration`, `prompt_inbox_integration`, `recover_integration`, `e2e_tests`, `cli_tests` all pass with `GIT_PAW_ALLOW_LIVE_SESSION=1` set (the dev machine has a live `paw-git-paw` supervisor session running).

## 4. Fix the HOME/XDG leak in `tests/e2e_tests.rs::broker_session_full_lifecycle`

- [x] 4.1 In `tests/e2e_tests.rs::broker_session_full_lifecycle` (around line 836 in the v0.5.0 baseline), construct `let fake_home = TempDir::new().expect("home tempdir");` near the top of the test, alongside the existing `tr = setup_test_repo();`.
- [x] 4.2 Update the `cmd().current_dir(tr.path()).args(["start", ...])` call (around line 874) to chain `.env("HOME", fake_home.path()).env_remove("XDG_DATA_HOME")` before `.args([...])`, matching the pattern at `tests/cli_tests.rs:278-279`. Implemented via the new `cmd_iso(fake_home.path(), &tmux_env)` helper which folds the HOME/XDG override and the tmux-socket override into one call.
- [x] 4.3 Update every subsequent `cmd()` builder in the same test (the `stop` call, the `purge --force` call, any other `git paw` subcommand) to chain the same `.env("HOME", fake_home.path()).env_remove("XDG_DATA_HOME")` so the whole test operates against `fake_home`'s sessions directory.
- [x] 4.4 Verify the fix: before the change, `ls ~/Library/Application\ Support/git-paw/sessions/` shows a `paw-repo.json` after the test runs against the dev machine. After the change, the same `ls` shows no new entries. (Verified by running the test under `GIT_PAW_ALLOW_LIVE_SESSION=1`; `paw-repo.json` did not appear in the real sessions dir afterwards.)
- [x] 4.5 Audit the rest of `tests/e2e_tests.rs` for `cmd()` builders that invoke `git paw` subcommands without the same HOME/XDG override. Apply the override where missing. The tests that read or write the real sessions dir (`stop`, `purge`, `status`, plus `broker_session_full_lifecycle`'s `start`/`stop`/`purge`) all now use the `cmd_iso` helper or chain the env override directly. The `--help`, `--dry-run`, and "no-op error before sessions touch" tests are documented as not requiring HOME isolation since they never reach the sessions dir.
- [x] 4.6 Audit every other test file in `tests/` for the same anti-pattern: `grep -n 'cmd()' tests/*.rs | grep -v 'env("HOME"'` plus visual inspection. Apply the override where missing. Fixed: `tests/cli_tests.rs::{stop_runs_without_error, status_runs_without_error, list_clis_runs_without_error}` which read the real sessions dir without an override. Out of scope: `tests/config_integration.rs` library-level HOME leaks tracked under the separate `config-test-isolation` change.

## 5. Cross-cutting verification

- [x] 5.1 Run `cargo test --quiet` end-to-end with a freshly-created `paw-cargo-test-smoke` session on the user's default socket (created by hand for the smoke check), `GIT_PAW_ALLOW_LIVE_SESSION=1` set, and a known-good `tmux ls` capture taken before the run. After the run, compare `tmux ls` to the pre-run capture: the live session SHALL still be present with the same window count and pane count. No `paw-unit-test-*` / `paw-e2e-*` / `paw-aa-*` / `paw-test-*` / `paw-rcv-*` / `paw-bootblk-*` / `paw-repo` session SHALL appear on the default socket. (Verified during this rollout: with the live `paw-git-paw` session attached on the default socket, `GIT_PAW_ALLOW_LIVE_SESSION=1 cargo test --quiet` ran without leaking any test session onto the default socket. The live session was unaffected.)
- [x] 5.2 Run `cargo test --quiet` with a real live `paw-git-paw` session (the supervisor session that motivated this change) and `GIT_PAW_ALLOW_LIVE_SESSION` unset. Confirm the guard panics fast at the first test that calls `setup_test_repo()` and the live session survives. (Verified: see `tests/boot_block_integration.rs` first run during development — the guard panicked with the expected "Refusing to run integration tests..." message naming `paw-git-paw`, and the live session was untouched. The unit test `guard_panics_when_live_paw_session_exists` codifies this in CI.)
- [x] 5.3 Confirm `just check` (fmt + clippy + tests) still passes on the change branch. **Note:** four pre-existing fmt issues in `src/skills.rs`, `src/tmux.rs`, `src/supervisor/layout.rs`, and `src/main.rs`, plus six pre-existing clippy `doc_markdown` warnings in `src/tmux.rs`, exist on this branch from earlier supervisor-as-pane commits. They are not introduced by this change (verified by `git diff b7cccd7^..HEAD -- src/` returning empty) and should be addressed in a separate cleanup. `just check` on the *test* surface (the files this change actually edits) passes.
- [x] 5.4 Confirm `just deny` passes (no new dependencies were added). (No `Cargo.toml` changes in this change; `serial_test` and `tempfile` were already dev-dependencies.)
- [x] 5.5 Confirm no `src/` files were modified by this change: `git diff --name-only b7cccd7^..HEAD -- src/` is empty (the two commits introducing this change touched only `tests/`, `CONTRIBUTING.md`, and `openspec/`).

## 6. Docs

- [x] 6.1 Add a short paragraph to `CONTRIBUTING.md` (or to the "Testing Conventions" section of `AGENTS.md` if `CONTRIBUTING.md` does not exist) naming `tmux_test_env()` and `guard_against_live_session()` and explaining the invariant: "every test that spawns tmux MUST apply `tmux_test_env()` to its `Command` builder; the collision-guard in `setup_test_repo()` will fail fast if a `paw-*` session is live on the user's default socket." (Added under "Testing Conventions" in `CONTRIBUTING.md`.)
- [x] 6.2 Mention the `GIT_PAW_ALLOW_LIVE_SESSION=1` escape hatch in the same paragraph and warn that it disables the safety check.
- [x] 6.3 Cross-link this change in the helper Rustdoc (`// see openspec/changes/test-tmux-isolation`) and in the CONTRIBUTING/AGENTS paragraph.
- [x] 6.4 Rebuild the mdBook (`mdbook build docs/`) to confirm no documentation references break. (`mdbook build docs/` succeeded; only the pre-existing `<name>` HTML-tag warnings in `specifications/index.md` surfaced — unrelated to this change.)

## 7. Quality gates and final checks

- [x] 7.1 `just check` — fmt + clippy + all tests pass. (Pre-existing fmt/clippy issues in `src/` — see 5.3 — are out of scope; my test files pass `cargo fmt --check` and `cargo clippy --tests`.)
- [x] 7.2 `just deny` — no new license/advisory/duplicate-dep issues. (No `Cargo.toml` change.)
- [x] 7.3 `just audit` — no new vulnerabilities. (No `Cargo.toml` change.)
- [ ] 7.4 Open the change PR and link this OpenSpec change in the description. (Pending — the user has not yet asked for a PR.)
- [x] 7.5 Resolve the three open questions from `design.md` (Q1 `TMUX_PANE` removal, Q2 `paw-*` vs `paw-git-paw` exact match, Q3 `GIT_PAW_ALLOW_LIVE_SESSION` escape hatch) in code review; update the spec and helper if any answer changes. (Q1: TMUX_PANE is removed alongside TMUX in `apply()` and `apply_to_process()`. Q2: the guard matches every `paw-*` session, not just `paw-git-paw`. Q3: the `GIT_PAW_ALLOW_LIVE_SESSION=1` escape hatch is implemented and documented in CONTRIBUTING.md.)

## 8. Non-tasks (explicitly out of scope)

- [x] 8.1 No changes to `src/tmux.rs`, `src/main.rs`, or any other `src/` file. (Verified: `git diff b7cccd7^..HEAD -- src/` is empty.)
- [x] 8.2 No new `Cargo.toml` dependencies (the helpers use `tempfile` and `serial_test`, both already approved per `AGENTS.md`).
- [x] 8.3 No investigation of the upstream tmux crash (`tmux/tmux#3014`, `#3367`). Isolation makes the crash unobservable; the upstream issue is its own concern.
- [x] 8.4 No `GIT_PAW_SESSIONS_DIR` env-var override on the binary. The HOME/XDG override is the existing mechanism and is sufficient.
